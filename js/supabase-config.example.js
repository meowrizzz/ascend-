/* =========================================================
   Ascend — Supabase config TEMPLATE
   ---------------------------------------------------------
   Copy this file to `js/supabase-config.js` and paste your
   own project values below.

   Where to find them:
   Supabase Dashboard → Project Settings → API
     • Project URL          → SUPABASE_URL
     • Project API keys → "publishable" (or anon) key → SUPABASE_PUBLISHABLE_KEY

   Use ONLY the publishable/anon key. Never put a secret key or
   the service_role key in client code. RLS must stay enabled.
   ========================================================= */
export const SUPABASE_URL = '';
export const SUPABASE_PUBLISHABLE_KEY = '';
