/* =========================================================
   Ascend — Supabase client configuration (PUBLIC)
   ---------------------------------------------------------
   This file is committed on purpose: the app is a static site
   on GitHub Pages, so the client needs these values at runtime.
   The PUBLISHABLE key is designed to be exposed in client code
   and is safe ONLY because Row Level Security (RLS) is enabled
   on the database.

   NEVER put a secret key or the service_role key here.
   ========================================================= */
export const SUPABASE_URL = 'https://mhnpotpmsayczzdgdkic.supabase.co';
export const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_EszhUnYbvO1BdbxKUuvZhw_Tq7yh82P';
